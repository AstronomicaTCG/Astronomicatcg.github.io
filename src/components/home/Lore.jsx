import React from "react";

const Lore = ({ heading }) => {
  return (
    <div>
        <div className="sectionHeading" id="Lore">
          <h2>{heading}</h2>
        </div>
    </div>
  );
};

export default Lore;
