import React from "react";

const Shop = ({ heading }) => {
  return (
    <div>
        <div className="sectionHeading" id="shop">
          <h2>{heading}</h2>
        </div>
    </div>
  );
};

export default Shop;
